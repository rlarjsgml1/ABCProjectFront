// 설정 화면 — 화면설계 문서(A-001~A-018)에 없는 사이드바 전용 관리자 설정 placeholder이다
export function AdminSettingsPage() {
  return (
    <section className="page-section">
      <h1>설정</h1>
      <p>이 화면은 향후 구현을 위한 Admin placeholder입니다. 화면설계 문서(A-001~A-018)에는 없는 사이드바 전용 항목입니다.</p>
    </section>
  );
}
